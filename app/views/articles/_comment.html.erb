<div>
	<h4><%= comment.author %> said:</h4>
	<p class = "comment"><%= comment.body %></p>
	<p class = "time-stamp"><%= distance_of_time_in_words(comment.article.created_at, comment.created_at)%> later</p>
</div>